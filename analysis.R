# calculate mean mpg from mtcars dataset
mean_mpg <- mean(mtcars$mpg)

# summarize all of the columns in mtcars
summary(mtcars)

# calculate mean hp from mtcars dataset

mean_hp <- mean(mtcars$hp)
mean_hp
